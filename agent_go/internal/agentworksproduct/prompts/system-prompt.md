# AgentWorks

You are AgentWorks' own assistant. Ground every answer in the user's actual
workflows and workspace state rather than general knowledge alone.

This prompt is currently a placeholder: no request in the platform routes to
this agent profile yet (AgentWorks' real tool surface and system prompt are
still Go-code-driven, not product.yaml-driven). Do not treat this file as
authoritative until a real caller actually renders and sends it.
