{
  "metadata_version": 1,
  "content_schema": "agentworks-playbook/v1",
  "id": "api-performance-validation",
  "version": "0.1.0",
  "title": "API Performance Validation",
  "description": "Measure approved API scenarios under bounded load against customer-defined latency, throughput, error, and capacity policies.",
  "hierarchy": [
    "AgentWorks",
    "Agentic Engineering Platform",
    "Performance Engineering"
  ],
  "order": 2,
  "entrypoint": "SKILL.md",
  "audience": "workflow_builder",
  "setup_prompt": "Use API Performance Validation for my authorized service environment. Preserve the exact target, authentication, scenarios, load limits, and budgets; run a bounded repeatable profile; retain sanitized measurements and diagnostics; and report regressions without changing policy.",
  "setup_inputs": [
    {
      "id": "target",
      "label": "Authorized API/service environment and exact build",
      "required": true
    },
    {
      "id": "scenarios",
      "label": "Endpoints, protocols, payload classes, and functional expectations",
      "required": true
    },
    {
      "id": "access",
      "label": "Authentication, test identities, and secret references",
      "required": true
    },
    {
      "id": "load_profile",
      "label": "Rate, concurrency, duration, connection, and stop limits",
      "required": true
    },
    {
      "id": "budgets",
      "label": "Metrics, thresholds or baseline, and decision policy",
      "required": true
    },
    {
      "id": "observability",
      "label": "Optional metrics, logs, and traces access",
      "required": false
    },
    {
      "id": "evidence_policy",
      "label": "Diagnostic redaction and retention policy",
      "required": true
    }
  ],
  "required_capabilities": [
    "api_test_execution",
    "bounded_load_generation",
    "performance_measurement",
    "durable_workflow_storage",
    "workflow_reporting",
    "human_decision"
  ],
  "recommended_tools": [
    {
      "id": "agentworks-scripted-execution",
      "name": "AgentWorks scripted execution",
      "type": "builtin",
      "purpose": "Run the saved API workload and deterministic result processing under the workflow contract",
      "capability": "api_test_execution",
      "optional": true
    },
    {
      "id": "load-test-runner",
      "name": "Customer-selected load-test CLI",
      "type": "cli",
      "purpose": "Generate the approved rate or concurrency profile with machine-readable results",
      "capability": "bounded_load_generation",
      "optional": true
    },
    {
      "id": "observability",
      "name": "Observability MCP",
      "type": "mcp",
      "purpose": "Correlate authorized service metrics, logs, or traces with a performance run",
      "capability": "performance_observability",
      "optional": true
    }
  ],
  "outputs": [
    "Versioned API performance policy and load profile",
    "Canonical saved API performance runner",
    "Raw samples and comparable aggregates",
    "Latency, throughput, error, and budget decisions",
    "Sanitized diagnostics and regression findings",
    "Live API performance report"
  ],
  "next_playbooks": [
    "scheduled-regression-synthetic-monitoring",
    "release-pr-quality-gate"
  ]
}
