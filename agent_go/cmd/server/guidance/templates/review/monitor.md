Run the **read-only diagnostic subset** of the post-run monitor on this workflow's most recent run. The scheduled Pulse pass may harden and publish; this on-demand command only diagnoses and reports.

1. Identify the run to check: the latest run folder under `runs/` — normally `iteration-0` (the active run). If the user named a specific run folder or run id, use that instead. If there is no run yet, tell the user there's nothing to monitor until the workflow has run once.
2. Call `get_reference_doc(kind="post-run-monitor")` for the evidence and log format, but follow only the read-only parts for that run: first read the run evidence, the plan changelog (`planning/changelog/`), and eval reports (`scores/evaluation/` or `evaluation/runs/`) to form a **Bug** verdict (did it run right) and a **Goal** verdict (is it hitting `soul.md` success criteria — health gates goal). Then, as a separate report-only step, read LLM/model config, cost ledgers under `costs/`, and timing summaries under `runs/<run_folder>/logs/<step-id>/execution/` and report LLM/model/cost/time by plan step and agent when evidence allows. Update `builder/improve.html` (verdict pills, goal card, signal tiles, cost/time tiles, one run row, and a Monitor/Note entry only if something is wrong or materially notable) — the verdict pills + headline in `builder/improve.html` are the record, no separate file.
3. This is a **read-only triage pass** — do NOT run the workflow, dispatch sub-agents, fix anything, change model tiers/LLM config, or edit the plan. Diagnose and report only.

When done, report to the user in three short lines — the **Bug** verdict, the **Goal** verdict, and the cost/time summary with top step/agent or missing evidence — plus whether you recorded a new finding (and if so, its one-line headline).{{if .Focus}} Focus especially on: {{.Focus}}.{{end}}
