apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workspace-projects-pvc
  namespace: prod-mcpagent
  labels:
    app.kubernetes.io/name: cs
    app.kubernetes.io/instance: mcpagent-workspace-projects-api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
