{
  "contract_version": "browser-journeys/v1",
  "revision": 1,
  "application_id": "example-portal",
  "approval_status": "example_only",
  "evidence_policy": {
    "video": "retain-on-failure",
    "console_log": "retain-errors-and-warnings",
    "network_log": "retain-failures-and-navigation",
    "network_response_bodies": "disabled",
    "redaction": "required_before_persistence"
  },
  "journeys": [
    {
      "id": "member-login",
      "name": "Member can log in",
      "required": true,
      "role": "member",
      "preconditions": ["Fresh unauthenticated browser context", "Authorized member account is available"],
      "actions": ["Open the login page", "Sign in using selected secret references"],
      "assertions": ["Workspace home identifies the expected member and workspace"],
      "expectation_source": "Example only; replace with an approved requirement",
      "cleanup": "Close the case-owned browser context; do not revoke shared account sessions"
    },
    {
      "id": "create-record",
      "name": "Member can create a record",
      "required": true,
      "role": "member",
      "preconditions": ["Independent authenticated context", "Disposable test workspace permits record creation"],
      "actions": ["Create a record with a unique per-run value", "Refresh and reopen that exact record"],
      "assertions": ["Exactly the intended record exists with the submitted value"],
      "expectation_source": "Example only; replace with an approved requirement",
      "cleanup": "Remove only this case's record through the approved fixture cleanup method"
    },
    {
      "id": "edit-record",
      "name": "Member can edit a record persistently",
      "required": true,
      "role": "member",
      "preconditions": ["Independent authenticated context", "A separately seeded case-owned record; no dependency on create-record"],
      "actions": ["Edit the seeded record", "Refresh and reopen it"],
      "assertions": ["The edited value persists on that record", "Unchanged fields retain their prior values"],
      "expectation_source": "Example only; replace with an approved requirement",
      "cleanup": "Remove only this case's seeded record through the approved fixture cleanup method"
    }
  ]
}
