{
  "contract_version": "browser-auth-scenarios/v1",
  "revision": 1,
  "application_id": "example-portal",
  "approval_status": "example_only",
  "scenarios": [
    {
      "id": "member-login-valid",
      "actor_role": "member",
      "initial_state": "fresh_unauthenticated_context",
      "action": "Sign in with the selected member secret references",
      "expected": "The expected workspace home is visible after authentication",
      "expectation_source": "Example only; replace with an approved requirement",
      "required": true,
      "side_effect_limit": "Do not change account or MFA settings",
      "cleanup": "Close the case-owned context"
    },
    {
      "id": "member-logout-protected-route",
      "actor_role": "member",
      "initial_state": "authenticated_case_owned_context",
      "action": "Log out and directly open one approved protected route",
      "expected": "The former session cannot access protected content",
      "expectation_source": "Example only; replace with an approved requirement",
      "required": true,
      "side_effect_limit": "Do not revoke unrelated sessions",
      "cleanup": "Close the case-owned context"
    }
  ],
  "evidence_policy": {
    "video": "retain-on-failure",
    "console_log": "retain-errors-and-warnings",
    "network_log": "retain-failures-and-navigation",
    "network_response_bodies": "disabled",
    "redaction": "required_before_persistence"
  }
}
