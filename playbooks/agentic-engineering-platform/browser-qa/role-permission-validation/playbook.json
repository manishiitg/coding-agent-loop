{
  "metadata_version": 1,
  "content_schema": "agentworks-playbook/v1",
  "id": "role-permission-validation",
  "version": "0.1.1",
  "title": "Role and Permission Validation",
  "description": "Validate page, action, and data permissions across roles, ownership states, and tenant boundaries.",
  "hierarchy": ["AgentWorks", "Agentic Engineering Platform", "Browser QA"],
  "order": 3,
  "entrypoint": "SKILL.md",
  "audience": "workflow_builder",
  "setup_prompt": "Use Role and Permission Validation with my authenticated browser foundation. Build an approved actor-resource-action matrix, verify allowed and denied paths including direct navigation, and retain redacted evidence without changing permissions.",
  "setup_inputs": [
    {"id": "foundation", "label": "Browser and authentication foundation", "required": true},
    {"id": "roles", "label": "Authorized test actors and roles", "required": true},
    {"id": "permission_matrix", "label": "Approved resources, actions, and expected decisions", "required": true},
    {"id": "test_data", "label": "Ownership, tenancy, fixture, and cleanup boundaries", "required": true},
    {"id": "evidence_policy", "label": "Evidence retention and redaction policy", "required": false, "default": "reuse_foundation"}
  ],
  "required_capabilities": ["browser_test_execution", "browser_recording", "browser_diagnostic_capture", "secret_injection", "durable_workflow_storage", "workflow_reporting"],
  "recommended_tools": [
    {"id": "playwright", "name": "Playwright CLI", "type": "cli", "purpose": "Execute the actor-resource-action matrix in isolated contexts", "capability": "browser_test_execution", "optional": true},
    {"id": "managed-agent-browser", "name": "AgentWorks managed agent-browser", "type": "builtin", "purpose": "Investigate permission mismatches and collect supported diagnostics", "capability": "browser_interaction", "optional": true},
    {"id": "identity-provider", "name": "Identity-provider MCP", "type": "mcp", "purpose": "Read explicitly authorized non-secret role configuration when available", "capability": "identity_provider_access", "optional": true}
  ],
  "pulse_focus": [
    {
      "module": "technical_review",
      "label": "Technical review",
      "focus_areas": [
        "Authorization outcomes across roles, resources, and tenants",
        "False-positive, evidence, and coverage integrity"
      ],
      "review_when": [
        "A run fails, produces incomplete evidence, or a key metric becomes unreliable"
      ]
    },
    {
      "module": "architecture_review",
      "label": "Architecture review",
      "focus_areas": [
        "Permission model, test identity isolation, and scenario reuse",
        "Tenant and ownership boundary design"
      ],
      "review_when": [
        "The workflow, integration, environment, scale, or operating policy changes materially"
      ]
    },
    {
      "module": "strategic_review",
      "label": "Strategic review",
      "focus_areas": [
        "Risk-based coverage of valuable actions and data",
        "Fitness of access-control signals for release decisions"
      ],
      "review_when": [
        "Outcome metrics regress, priorities change, or the current approach needs periodic challenge"
      ]
    }
  ],
  "outputs": ["Approved permission matrix", "Canonical permission tests", "Complete matrix execution history", "Attempt-scoped video and console/network evidence", "Permission findings and live report", "Verified non-secret application knowledge"],
  "setup_playbooks": ["basic-browser-setup", "authentication-session-validation"],
  "accepts_equivalent_setup": true,
  "next_playbooks": ["critical-journey-validation"]
}
