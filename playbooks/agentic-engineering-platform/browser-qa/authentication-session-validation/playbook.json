{
  "metadata_version": 1,
  "content_schema": "agentworks-playbook/v1",
  "id": "authentication-session-validation",
  "version": "0.1.1",
  "title": "Authentication and Session Validation",
  "description": "Validate login, logout, MFA, recovery, expiry, refresh, and invalid-session behavior with durable evidence.",
  "hierarchy": ["AgentWorks", "Agentic Engineering Platform", "Browser QA"],
  "order": 2,
  "entrypoint": "SKILL.md",
  "audience": "workflow_builder",
  "setup_prompt": "Use Authentication and Session Validation with my browser foundation. Preserve my authentication provider and policies, create only authorized scenarios, run them from controlled contexts, and retain redacted video and console/network evidence.",
  "setup_inputs": [
    {"id": "foundation", "label": "Browser foundation or equivalent setup", "required": true},
    {"id": "auth_methods", "label": "Authentication and session methods", "required": true},
    {"id": "actors", "label": "Authorized test actors and account ownership", "required": true},
    {"id": "expectations", "label": "Approved authentication expectations", "required": true},
    {"id": "safety_limits", "label": "Lockout, recovery, MFA, and mutation limits", "required": true},
    {"id": "evidence_policy", "label": "Evidence retention and redaction policy", "required": false, "default": "reuse_foundation"}
  ],
  "required_capabilities": ["browser_test_execution", "browser_recording", "browser_diagnostic_capture", "secret_injection", "durable_workflow_storage", "workflow_reporting"],
  "recommended_tools": [
    {"id": "playwright", "name": "Playwright CLI", "type": "cli", "purpose": "Run repeatable authentication scenarios in controlled browser contexts", "capability": "browser_test_execution", "optional": true},
    {"id": "managed-agent-browser", "name": "AgentWorks managed agent-browser", "type": "builtin", "purpose": "Investigate authorized authentication states and collect supported diagnostics", "capability": "browser_interaction", "optional": true},
    {"id": "identity-provider", "name": "Identity-provider MCP", "type": "mcp", "purpose": "Use an explicitly configured provider integration for approved setup or evidence", "capability": "identity_provider_access", "optional": true}
  ],
  "pulse_focus": [
    {
      "module": "technical_review",
      "label": "Technical review",
      "focus_areas": [
        "Authentication behavior, expiry, refresh, and recovery correctness",
        "Sensitive evidence redaction and failure classification"
      ],
      "review_when": [
        "A run fails, produces incomplete evidence, or a key metric becomes unreliable"
      ]
    },
    {
      "module": "architecture_review",
      "label": "Architecture review",
      "focus_areas": [
        "Session-state model, identity boundaries, and reusable authentication setup",
        "Isolation of credentials, roles, and environments"
      ],
      "review_when": [
        "The workflow, integration, environment, scale, or operating policy changes materially"
      ]
    },
    {
      "module": "strategic_review",
      "label": "Strategic review",
      "focus_areas": [
        "Alignment of authentication coverage with customer and business risk",
        "Usefulness of session metrics and release policy"
      ],
      "review_when": [
        "Outcome metrics regress, priorities change, or the current approach needs periodic challenge"
      ]
    }
  ],
  "outputs": ["Approved authentication scenario manifest", "Canonical authentication tests", "Actor/scenario run history", "Attempt-scoped video and console/network evidence", "Authentication findings and live report", "Verified non-secret application knowledge"],
  "setup_playbook": "basic-browser-setup",
  "accepts_equivalent_setup": true,
  "next_playbooks": ["role-permission-validation", "critical-journey-validation"]
}
