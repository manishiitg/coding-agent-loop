{
  "metadata_version": 1,
  "content_schema": "agentworks-playbook/v1",
  "id": "release-pr-quality-gate",
  "version": "0.1.1",
  "title": "Release and PR Quality Gate",
  "description": "Bind an exact change or build to required Browser QA suites and publish an auditable pass, fail, or needs-review decision.",
  "hierarchy": ["AgentWorks", "Agentic Engineering Platform", "Browser QA"],
  "order": 8,
  "entrypoint": "SKILL.md",
  "audience": "workflow_builder",
  "setup_prompt": "Use Release and PR Quality Gate with my proven Browser QA suites. Bind the exact change and tested build, require complete selected suite results, derive the decision from my policy, and publish only to authorized destinations with real receipts.",
  "setup_inputs": [
    {"id": "trigger", "label": "Manual, authenticated API, PR, or release trigger", "required": true},
    {"id": "identity", "label": "Repository change, build, and environment identity mapping", "required": true},
    {"id": "required_suites", "label": "Required suites, routes, and groups", "required": true},
    {"id": "policy", "label": "Blocking, incomplete, rerun, and review policy", "required": true},
    {"id": "evidence_policy", "label": "Gate and suite evidence retention", "required": true},
    {"id": "delivery", "label": "Authorized status, report, and notification destinations", "required": false}
  ],
  "required_capabilities": ["workflow_trigger", "browser_test_execution", "durable_workflow_storage", "workflow_reporting", "quality_gate_decision"],
  "recommended_tools": [
    {"id": "agentworks-api-trigger", "name": "AgentWorks API trigger", "type": "builtin", "purpose": "Start the saved gate route from an authenticated CI or repository event", "capability": "workflow_trigger", "optional": true},
    {"id": "github", "name": "GitHub CLI or MCP", "type": "cli_or_mcp", "purpose": "Resolve authorized change context and publish an approved check or summary", "capability": "change_context_and_reporting", "optional": true},
    {"id": "notification-destination", "name": "Notification MCP", "type": "mcp", "purpose": "Deliver explicitly configured gate outcomes", "capability": "notifications", "optional": true}
  ],
  "pulse_focus": [
    {
      "module": "technical_review",
      "label": "Technical review",
      "focus_areas": [
        "Change-to-test binding, gate calculation, and evidence integrity",
        "Failure, override, and stale-result handling"
      ],
      "review_when": [
        "A run fails, produces incomplete evidence, or a key metric becomes unreliable"
      ]
    },
    {
      "module": "architecture_review",
      "label": "Architecture review",
      "focus_areas": [
        "Gate placement, required suite composition, and provider integration",
        "Approval, retry, and audit architecture"
      ],
      "review_when": [
        "The workflow, integration, environment, scale, or operating policy changes materially"
      ]
    },
    {
      "module": "strategic_review",
      "label": "Strategic review",
      "focus_areas": [
        "Alignment of gate policy with release risk and delivery speed",
        "Value of required checks and override policy"
      ],
      "review_when": [
        "Outcome metrics regress, priorities change, or the current approach needs periodic challenge"
      ]
    }
  ],
  "outputs": ["Versioned release-gate policy", "Exact trigger/change/build mapping", "Complete required-suite result set", "Auditable pass/fail/needs-review decision", "Gate report and evidence links", "External delivery receipts when configured"],
  "setup_playbooks": ["critical-journey-validation"],
  "accepts_equivalent_setup": true
}
