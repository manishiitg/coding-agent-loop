---
name: guides
description: Technical page and diagram references, read when needed with read_skill(name "guides", path "<file>").
---

# Shared guides

Read a guide with `read_skill` using name `guides` and the file as `path`:

- `html-design.md` — page bridge, images, answer widgets, timers and figure checks.
- `diagrams.md` — a compact JSXGraph reference for accurate figures.

For how to write a good `image_gen`/`image_edit` prompt, read the separate
`image-prompting` skill (not part of this bundle — it's a general platform
skill, not SparkQuill-specific).
